{% extends 'admin/base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>ImagePath</th>
                <td>{{ post.imagePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_post_list') }}">back to list</a>

    <a href="{{ path('admin_post_edit', {'id': post.id}) }}">edit</a>

<<<<<<< HEAD
            <div class="col-sm-12 col-md-6 col-lg-6">

                <div class="card">
                    <img src="{{ post.imagePath }}" class="card-img-top imageCard" alt="image_category">
                    <div class="card-body">
                        <h5 class="card-title">{{ post.title }}</h5>
                        {{ post.content | raw}}
                       
                    </div>
                </div>
        
            </div>

            <div class="col-sm-12 col-md-6 col-lg-6">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Configuration</h5>
                        <p>Créé le {{ post.createdAt | date('d-M-Y') }}</p>
                        <p>Statut : {{ post.status }}</p>
                    </div>
                    <div class="card-footer">
                        {% if post.status is same as 'Brouillon' %}
                            <a href="{{ path('admin_post_toggle_status',{'id': post.id}) }}" class="btn btn-info w-100">Publié le post</a>
                        {% else %}
                            <a href="{{ path('admin_post_toggle_status',{'id': post.id}) }}" class="btn btn-info w-100">Passer en brouillon</a>
                        {% endif %}     
                        
    <a class="btn btn-outline-dark" href="{{ path('admin_post_list') }}">back to list</a>

    <a class="btn btn-outline-dark" href="{{ path('admin_post_edit', {'id': post.id}) }}">edit</a>

    {{ include('admin/post/_delete_form.html.twig') }}
                    </div>
                </div>
        
            </div>

            


       

    </div>

  </div>
{% endblock %}
=======
    {{ include('admin/post/_delete_form.html.twig') }}
{% endblock %}
>>>>>>> 64ad6fc221e51f09c92e26c26af75b6b7c4766f8
