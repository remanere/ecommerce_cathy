{% extends "base.html.twig" %}

{% block body %}

  <section>
      <div class="container py-5">
        <div class="row">
     
              {% include "_messages_flash.html.twig" %}

            <div class="col-sm-6 col-md-6">

                <div class="card">
                   <div class="card bg-dark">
                    <img src="{{ product.imagePath }}" class="card-img imageDetailProduct" alt="image_category">
                </div>
                
            </div>
            </div>
            <div class="col-sm-12 col-md-6">

                   <h5 class="card-title">{{ product.name }}</h5>
          
            <p>{{ product.contenu }}</p>

                  <a class="btn btn-outline-dark" href="{{ path('home') }}">Revenir à l'acceuil</a>
                
            </div>

        </div>
    </div>

  </section>

   <section>
   <hr>
    <div class="container py-5">
        <div class="row">
            <div class="col-12">
                <h3 class="text-center">Suggestions</h3>
            </div>  

            {% for product in suggestedProducts %}

                <div class="col-sm-12 col-md-6 col-lg-3 mt-3">

                    <div class="card bg-dark text-dark text-center">
                        <img src="{{ product.imagePath }}" class="card-img imageCard" alt="image_category">
                        <div class="card-img-overlay">
                            <h5 class="card-title">{{ product.name }}</h5>
                            <a href="{{ path('boutique_product_detail',{'id': product.id} ) }}" class="btn btn-outline-light">Consulter</a>
                        </div>
                    </div>
            
                </div>
                
            {% endfor %}

        </div>
    </div>
  </section>

{% endblock %}