{% extends "base.html.twig" %}

{% block body %}

  <section>
      <div class="container py-5">
        <div class="row">
     
              {% include "_messages_flash.html.twig" %}

            <div class="col-sm-6 col-md-6">

                <div class="">
                   <div class="bg-dark">
                    <img src="{{ product.imagePath }}" class="card-img imageDetailProduct" alt="image_category">
                </div>
                
            </div>
            </div>
            <div class="col-sm-12 col-md-6">

                   <h5 class="card-title">{{ product.name }}</h5>
          
            <p>{{ product.contenu }}</p>
            <hr>
                  <a class="btn btn-outline-dark" href="{{ path('home') }}">Revenir à l'accueil</a>
                 <hr>
            
              <a class="btn btn-outline-dark" href="mailto:amelie.factory@outlook.fr">Pour toute commande, c'est ici</a>
               
                
            </div>

        </div>
    </div>

  </section>

   <section>
 
    <div class="container py-5">
      <hr>
        <div class="row">
            <div class="col-12">
                <h3 class="text-center">Suggestions</h3>
            </div>  

            {% for product in suggestedProducts %}

             <div class="col-sm-12 col-lg-3 mt-2">
             <div class="card">
              <img src="{{ product.imagePath }}" class="card-img imageCard" alt="image_category">
              <div class="card-content">
                <h2 class="card-title">{{ product.name }}</h2>
                <a href="{{ path('boutique_product_detail',{'id': product.id} ) }}" class="btn btn-outline-light btn-lg">Consulter</a>
              </div>
            </div>
          </div>
          
            {% endfor %}

        </div>
    </div>
  </section>

{% endblock %}